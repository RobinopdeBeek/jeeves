import type { StepPanelProps } from "./step-panel-types";

/** Spec tab stub — editor arrives in slice 6. */
export function StepSpec(_props: StepPanelProps) {
  return (
    <div className="flex flex-1 flex-col items-center justify-center gap-2 p-4 text-center">
      <p className="text-muted-foreground">Spec editor coming soon.</p>
    </div>
  );
}
