# Model picker per step

> Future feature — still refining.

Each step/run kind can use a different model (Grill, Spec, research, implement, …). Neat UI to set the model per kind; stay current as new models ship.

## Ideas

- Per-step (or per-run-kind) model defaults
- Override for a single run without changing the default
- Easy refresh when new models appear
