# Repo picker + creator

> Future feature — still refining.

Switch between known target repos and create/register new ones easily. Active project stays singular (`projects.repo_path` → `<repo>/.jeeves/`).

## Ideas

- Picker: list known repos, switch active project / store
- Register existing local path
- Create new repo (init / template — TBD), then register
- Show which repo is active in chrome
