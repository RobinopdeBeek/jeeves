## Parent

Part of #55 (Slice 8: Full task pipeline).

## What to build

When a feature fans out via Implement →, the feature branch is created and recorded, and only unblocked child tasks get Plan queued and enqueued. Blocked children stay pending until merge (later slice). Adapters dispatch `ensure-branch` and per-child enqueue effects; CardStore stays git-free.

## Agent size

M — PipelineEngine effects + CardStore fan-out + effect dispatch; established patterns.

## Verification

CI only

## Acceptance criteria

- [ ] Fan-out declares `ensure-branch` for the feature and `enqueue` effects that name the target child card id
- [ ] Feature durable branch is created/recorded at fan-out (`cards.branch` set)
- [ ] Children with no unmerged blockers get `plan: queued` and are enqueued
- [ ] Children with blockers keep `plan: pending` and are not enqueued
- [ ] Effect dispatch runs ensure-branch and child enqueues (not only parent card id)
- [ ] Second fan-out still rejected (existing 409 behavior preserved)

## Blocked by

- Slice 8.1 (safe worktree base resolution)
