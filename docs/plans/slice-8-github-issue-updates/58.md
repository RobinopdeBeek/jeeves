## Parent

Part of #55 (Slice 8: Full task pipeline).

## What to build

Plan becomes a real slice-planning step (light planning like `/implement`, Context7 when available later). On Plan success, Implement is queued on the same card so the pipeline advances unattended. Retire the slice-3 tracer as the Plan skill for production runs.

Injected prompt inputs are **pipeline artifacts and card fields** only (parent spec when child, manifest, etc.). **Card attachments** from the Info library (#54) are **not** injected in this slice — deferred.

## Agent size

M — real plan-implementation prompt/policy + step-finished plan→impl chain.

## Verification

CI mocks + manual spike

## Acceptance criteria

- [ ] Plan step uses `plan-implementation` (tracer no longer the Plan skill)
- [ ] Plan postcondition: non-empty plan artifact harvested; no source commits
- [ ] Prompt receives injected pipeline inputs (card fields, parent spec when child, manifest) — agent does not hunt the DB
- [ ] Card attachments / Info library files are **not** required in the Plan prompt (deferred)
- [ ] On Plan success: `plan` done, `impl` queued, enqueue Implement for that card
- [ ] On Plan failure: step `needs-user`, Implement not enqueued; Retry still works
- [ ] Standalone Implement now → and fanned-out child Plan both exercise this path

## Blocked by

- Slice 8.1 (safe worktree base resolution)
- Slice 8.2 for the child fan-out path (standalone can proceed after 8.1 alone)
