## Parent

Part of #55 (Slice 8: Full task pipeline).

## What to build

Implement runs for real: follows the plan with TDD at pre-agreed seams, commits on the card branch, then the host runs Jeeves-owned verify commands. A red verify fails the step to needs-user. On success, AI Review can be queued by the next ticket's chain (this ticket at least leaves Implement done and ready to chain).

Prompt inputs: plan artifact (+ card fields as already patterned). **Card attachments** from #54 stay **out of scope** for this slice.

## Agent size

M — implement policy/prompt + `projects.verify_commands` host gate.

## Verification

CI mocks + manual spike

## Acceptance criteria

- [ ] Implement step policy: ≥1 commit on card branch, clean tree after exchange cleanup
- [ ] Continuation run uses checkout-existing (Implement commits survive; depends on 8.1)
- [ ] `projects.verify_commands` stored as ordered Jeeves-owned commands; null/empty skips with warning
- [ ] After successful agent finalize, host runs verify_commands in the worktree; non-zero → step failed/`needs-user` with log evidence
- [ ] On Implement success: `impl` done and next step can be queued (chain to AI Review may land in 8.4 if not this ticket — if chained here, airev queued)
- [ ] Prompt is real `implement-task` (plan injected); not a fixture-only commit skill
- [ ] Card attachments / Info library files are **not** required in the Implement prompt (deferred)

## Blocked by

- Slice 8.3a (Plan succeeds and queues Implement)
