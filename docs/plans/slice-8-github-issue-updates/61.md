## Parent

Part of #55 (Slice 8: Full task pipeline).

## What to build

The Review column opens with an explicit Prepare Eval step before human Review. In this ticket Prepare Eval is a stub that can finish so the demo reaches human Review needs-user. While working, the UI shows “Preparing interactive evaluation…”. Real eval-assemble is slice 9.

When adding step key `prepeval`, also extend any **parallel step-key enums** introduced by #54 that list the same keys (notably `cardAttachments.originStep` in `server/db/schema.ts`) so schema stays consistent even if attachments are not used on this step yet.

## Agent size

M — new `prepeval` step key + stub policy + thin UI copy + enum parity.

## Verification

CI only

## Acceptance criteria

- [ ] Step key `prepeval` (Prepare Eval) exists in the Review column before human `review`
- [ ] Parallel step-key enums from #54 (e.g. `cardAttachments.originStep`) include `prepeval`
- [ ] After AI Review success, `prepeval` is queued/enqueued and human `review` stays pending until prep finishes
- [ ] Stub Prepare Eval meets a thin postcondition (placeholder eval or agreed stub artifact) with no source commits
- [ ] On stub success: `prepeval` done, human `review` → `needs-user`
- [ ] UI shows preparing copy while Prepare Eval is `ai-working`
- [ ] Human Review tab remains a stub surface (no full QA gate / iframe polish — slice 9)

## Blocked by

- Slice 8.4 (AI Review review + one-pass rework)
