## Parent

Part of #55 (Slice 8: Full task pipeline).

## What to build

The execution queue runs unblocked siblings depth-first: task 1’s Plan → Implement → AI Review (and prep) before task 2’s Plan. Order is derived from card_steps + blockers + sibling position + step index, rebuilt on boot — not an in-memory front-insert that disappears on restart.

Unrelated to #54 Chat work; still required for the unattended multi-child demo.

## Agent size

S — derived ordering at an existing ExecutionEngine/CardStore seam; no new module.

## Verification

CI only

## Acceptance criteria

- [ ] Eligible steps: `queued` and no unmerged blockers
- [ ] Order: sibling `position`, then step index `plan < impl < airev < prepeval` (depth-first per task)
- [ ] Boot rebuild uses the same ordering (restart does not become plans-first FIFO)
- [ ] Blocked children never start even if incorrectly marked queued
- [ ] With two unblocked children, observed run order completes child 1’s Implement-column steps before child 2’s Plan

## Blocked by

- Slice 8.2 (Fan-out ignites unblocked children)
- Slice 8.3a (Plan succeeds and queues Implement)
