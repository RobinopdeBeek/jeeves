## Parent

Part of #55 (Slice 8: Full task pipeline).

## What to build

When Implement is done, the Implement tab keeps the live/frozen run-log pattern and underneath shows a rich changes diff: file list with stats first, lazy per-file patches, assistant-ui DiffViewer (unified default), live three-dot range vs upstream (feature branch or default_branch) so AI Review rework appears when viewed later. Soft size warning with Load anyway for large diffs.

**Dependency note (post #54):** `@assistant-ui/react` is present, but DiffViewer is **not** wired or guaranteed available from current packages. This ticket must add whatever assistant-ui DiffViewer package/export the design requires (confirm against current assistant-ui docs), then use it. Do not invent a bespoke diff engine.

## Agent size

M — first DiffViewer usage + package add + diff API (one novel UI concept).

## Verification

CI mocks + manual spike

## Acceptance criteria

- [ ] DiffViewer dependency/export added and usable from the client (not assumed present after #54)
- [ ] Diff API: list changed files + stats for `upstream...cardBranch` at view time
- [ ] Diff API: fetch per-file patch lazily
- [ ] Upstream is parent feature branch (child) or `projects.default_branch` (standalone)
- [ ] Implement tab: StepExecution-style log; when done, DiffViewer body (not plan markdown)
- [ ] Soft total-line (or equivalent) warning with Load anyway
- [ ] Resolved base/tip SHAs visible or otherwise attributable in the UI
- [ ] Plan and AI Review tabs remain log + markdown artifact (unchanged pattern)

## Blocked by

- Slice 8.3b (Implement commits + verify_commands gate)
