## Parent

Part of #55 (Slice 8: Full task pipeline).

## What to build

SDK local agent runs enable the settings sources needed for MCP (including Context7) so Plan can do light planning like `/implement`. If Context7/MCP is unavailable, Plan continues (non-fatal), optionally noting that docs were not fetched.

Still accurate after #54: `cursor-sdk-runner` still forces `settingSources: []`. This is execution-runner scope, not ACP Chat.

## Agent size

S — runner options only; no new module.

## Verification

CI mocks + manual spike

## Acceptance criteria

- [ ] Agent runner no longer forces empty settingSources that block all MCP
- [ ] Documented which sources are enabled for local runs
- [ ] Plan (and other execution steps) do not fail solely because Context7 is missing
- [ ] With Context7 configured on the host, a manual spike can observe MCP use during Plan (best-effort evidence)

## Blocked by

- Slice 8.1 (safe worktree base resolution) — can proceed in parallel with later tickets once 8.1 lands
